import React from 'react';
import { Typography, Box } from '@mui/material';

function Scheduler() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Scheduler
      </Typography>
      {/* Scheduler content will go here */}
    </Box>
  );
}

export default Scheduler;